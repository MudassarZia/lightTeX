#pragma once

// Plugin system stub (v0.4)
// Sol2 + LuaJIT for Lua scripting with vim.api.* compatibility.

namespace lighttex::plugin {

class PluginManager {
public:
    PluginManager() = default;
    // TODO (v0.4): Sol2 + LuaJIT plugin system
};

} // namespace lighttex::plugin
