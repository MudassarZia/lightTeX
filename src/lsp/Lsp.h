#pragma once

// LSP integration stub (v0.2)
// Will use QProcess + JSON-RPC for texlab integration.

namespace lighttex::lsp {

class LspClient {
public:
    LspClient() = default;
    // TODO (v0.2): QProcess-based texlab integration
};

} // namespace lighttex::lsp
