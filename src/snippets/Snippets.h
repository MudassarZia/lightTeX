#pragma once

// Snippets stub (v0.2)
// TOML-defined LaTeX snippets with tab-trigger expansion.

namespace lighttex::snippets {

class SnippetManager {
public:
    SnippetManager() = default;
    // TODO (v0.2): TOML snippet definitions, tab-trigger
};

} // namespace lighttex::snippets
